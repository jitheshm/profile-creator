import RegistationForm from "@/components/RegistationForm";

/**
 * Home component that renders the registration form.
 *
 * @returns {JSX.Element} A JSX element representing the home page with the registration form.
 */
export default function Home() {
  return <RegistationForm />;
}
